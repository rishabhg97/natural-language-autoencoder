schema_version: nano_av_probe_queue.v1
defaults:
  code_root: /workspace/interp/code/nano30b-nla-pilot-publication-current
  python: /workspace/interp/.venv/bin/python
  validation_limit: 512
  test_limit: 512
  eval_splits: [validation]
  generation_examples: 8
  cleanup_converted_hf_after_eval: true
  converted_hf_dtype: bfloat16
  cleanup_dcp_model_after_eval: false
  study_jsonl: /workspace/interp/outputs/nano30b-nla-pilot/publication/studies/r33_deterministic_family_clean_av_trials.jsonl
items:
  - name: r33-publication-deterministic-family-clean-av-sft-8gpu-pospass-dyn4096
    config: configs/nano_av/publication/r33_family_clean_sft_8gpu_dyn4096.yaml
    run_id: nano-av-r33-publication-deterministic-family-clean-8gpu-pospass-lr1e4-warmup25-gb192-mb2-dyn4096
    status: pending
    skip_training_if_checkpoint_exists: true
    study_task: av
    roundtrip_enabled: false
    notes: >-
      Publication-clean family-disjoint AV SFT. The fail-closed packed-vs-padded
      gate requires packed position resets to reach both Mamba and attention.
      Dyn4096 passed two optimizer updates on four H100-NVL GPUs and is promoted
      to eight GPUs without changing gb192, LR, warmup, data, or eval protocol.
