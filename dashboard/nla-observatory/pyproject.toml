[project]
name = "nla-observatory-data"
version = "0.1.0"
description = "Pinned static-data builder environment for the NLA Offline Observatory"
requires-python = ">=3.12,<3.13"
dependencies = [
  "numpy==2.1.0",
  "pyarrow==23.0.1",
  "tokenizers==0.22.1",
]

[tool.uv]
package = false
